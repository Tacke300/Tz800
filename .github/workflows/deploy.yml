name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 34.145.69.100
          username: Tacke300
          key: |
            ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIJd/M5P0gpU1rPzUoLeiRJ9g7DXVIDwP3cBhVnhJ9hTc deploy@yourdomain.com
          port: 22
          script: |
            cd ~
            git pull origin main
            systemctl restart tz800.service
