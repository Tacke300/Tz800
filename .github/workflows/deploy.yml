name: Auto Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy code to VPS via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 34.145.69.100
          username: tacke300
          key: ${{ secrets.VPS_KEY }}
          script: |
            cd /home/tacke300/fundingbot
            git pull origin main
            # Nếu bạn cần restart service sau khi cập nhật, thêm lệnh ở đây, ví dụ:
            # systemctl restart fundingbot
